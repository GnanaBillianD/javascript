//add 10 elements to the page
for (let i=0; i<10; i++){
    //create a new HTML element
    let element = document.createElement("div");
    element.innerHTML = "element" + 1;
    //document.appendChild(element);
}

//check the out in console
